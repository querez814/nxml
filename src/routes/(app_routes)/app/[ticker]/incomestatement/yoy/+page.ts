/*import type { PageLoad } from './$types';
import { fetchIncomeStatementYoY } from '../../../../../../api/incomesheet/incomedata';
export const load = (async ({ params }) => {
	const ticker = params.ticker;
	const quarters = await fetchIncomeStatementYoY(ticker);
	return { quarters };
}) satisfies PageLoad;
*/
